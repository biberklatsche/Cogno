import { CommandSpec } from "../../spec.types";

export const SEVEN_Z_FIG_SPEC: CommandSpec = {
    name: "7z",
    source: "fig",
    subcommands: ["a", "x", "e", "l", "t", "u", "d", "-p", "-o", "-y"],
};
